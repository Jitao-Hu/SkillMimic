## 本周工作总结（过去 7 天）

**时间范围**：2026-03-06 至 2026-03-12  
**项目**：SkillMimic – Dual Humanoid HRL

---

### 1. 训练算法与 HRL 结构改动

- **调整 HRL 训练超参数与结构**  
  - **更新超参数**：`entropy_coef`, `learning_rate`, `imit_reward`, `horizon_len`, `minibatch_size` 等，围绕 dual humanoid HRL 做了一轮系统性调参。  
  - **训练稳定性与样本效率**：多次迭代 early termination 开关、episode 最大长度、LLC 步长和 ground contact 惩罚上限，在“训练稳定”和“探索足够”之间寻找平衡。  
  - **HRL agent 逻辑增强**：在 `hrl_dual_agent.py` 中新增 skill monitor 模块，对高层技能选择（A 的 pass/run/idle，B 的 catch/run/idle）进行统计与可视化，方便在 TensorBoard/W&B 中分析策略。

---

### 2. 任务和环境逻辑增强

- **协作奖励函数精细化**  
  - 强化接球相关奖励：提高 `catch_success` 权重，并新增 `catch_fail` 惩罚，当球在 B 附近落地且未成功接球时给予负反馈。  
  - 为防止机器人倒地导致奖励爆炸，对非脚部接触地面的惩罚做上限裁剪，避免出现极端负奖励。  

- **任务观测空间扩展到 21 维**  
  - A 侧观测：在 A 的 heading 坐标系下，感知球相对 B 手部的位置以及 B 的线速度。  
  - B 侧观测：在 B 的 heading 坐标系下，引入球相对 root 的位置、球速度、球相对 B 手部中心的位置、球相对 B 手部的相对速度，以及约 0.5 秒后的弹道预测落点。  
  - 通过更丰富的任务特征，让 B 在迎球和预判落点上有更强的感知能力。

---

### 3. 统计与监控基础设施

- **Skill Monitor（技能使用监控）**  
  - 统计各类技能在关键条件下的使用频率，例如：  
    - 球在 A 手中时，A 选择 pass 的比例。  
    - 球飞向 B 时，B 选择 catch 或 run 的比例。  
  - 将上述比率与条件概率（如 `p_B_catch_given_ball_to_B`、`p_A_pass_given_A_holding`）写入 TensorBoard，便于分析高层策略是否合理利用了技能空间。

- **Per-Attempt Catch Stats（按球次的抓球统计）**  
  - 在环境中新增按「每一次球飞行（ball flight）」粒度的统计：  
    - 记录 pass attempt/success、catch attempt/success、catch fail 的次数与比率。  
    - 同时统计 alive、standing、upright、ground contact 等姿态相关的 step 级别占比。  
  - 在 agent 中定期从环境读取并写入 TensorBoard/W&B，并按窗口重置，使得训练过程中可以持续追踪传接球成功率的演化。

---

### 4. 配置、脚本与实验管理

- **配置文件更新**  
  - 对 `hrl_dual_humanoid.yaml` 与训练配置多次迭代：  
    - 调整 reward 权重（特别是与接球稳定性相关的项）。  
    - 同步更新 `goalSize` 与新的 task obs 结构。  
    - 多次调整 `llc_steps`，在高层决策频率与低层平滑控制之间做权衡。

- **运行脚本与实验目录**  
  - 将推理解码日志统一输出到 `inference_log/` 目录，便于与训练日志区分。  
  - 更新默认使用的 checkpoint 路径，使脚本始终指向最新一轮实验结果。  
  - 在 `run.py` 中改造 experiment 命名规则：目录名加入完整年月日与时间戳，并在 `resume_from` 时复用原有 experiment 目录，保证长时间训练的结果集中管理。  

- **WandB 数据导出工具**  
  - 新增 `wandb_csv.py` 脚本，将指定 run 的历史曲线、config 和 summary 导出为本地 CSV/JSON，方便做离线分析和交叉对比。

---

### 5. 文档与知识沉淀

- **归档三份中文训练指南**  
  - 在 `archive/` 目录下撰写并归档：  
    - 双人传接球 HRL 分步优化计划。  
    - 提升 B 接球稳定性的专项指南。  
    - 整体 SkillMimic 传接球任务的优化思路总结。  
  - 这些文档系统化记录了从 reward 设计、观测空间扩展，到技能引导与课程学习的调参路径，方便后续复现与团队协作。

---

### 总体小结

本周围绕 Dual Humanoid HRL 传接球任务，完成了从 **算法调参 → 奖励/观测设计 → 统计监控 → 实验管理与文档沉淀** 的一整套闭环：  
- 在训练超参数和任务逻辑层面，反复调整 early termination、llc_steps、reward 权重和 ground contact 惩罚，使策略在稳定性与探索性之间达到更好的平衡。  
- 在观测与统计层面，通过扩展 21 维 task obs、引入 Skill Monitor 与 per-attempt catch stats，让「策略在做什么」和「结果有多好」都以量化指标的形式暴露出来。  
- 在工程与文档层面，改进运行脚本、实验命名规则和 WandB 导出工具，并同步整理中文优化指南，为后续持续迭代和团队协作打下基础。

